# vibe-order-infra

Учебная инфраструктура для безопасного приёма и обработки клиентских заявок.

Инфраструктура развёрнута на VPS и прошла реальный smoke-test —
подробности см. в разделе "Статус деплоя" ниже. Локальный репозиторий
остаётся источником истины для конфигурации: секреты (`.env`,
`registry/auth/htpasswd`, TLS-ключи) на VPS создаются/монтируются
отдельно и в git не попадают.

## Целевой сервер

- Ubuntu 24.04, Docker Engine 29.6.2, Docker Compose plugin v5.3.1
- Публичный домен: `vibe.elivcloud.org`
- Registry: `registry-vibe.elivcloud.org`
- UFW пропускает снаружи только 22, 80, 443

**UFW — не единственный механизм защиты.** Опубликованные Docker-порты
(`ports:` в compose) обходят UFW напрямую: Docker сам прописывает
правила в iptables (цепочка `DOCKER`), которые в стандартной
конфигурации применяются РАНЬШЕ пользовательских правил UFW. Поэтому
"UFW разрешает только 22/80/443" не защищает от сервиса, который
случайно опубликован на `0.0.0.0:5432` — такой порт был бы доступен
снаружи, несмотря на UFW. Единственный надёжный способ — вообще не
публиковать чувствительные порты на host (см. ниже), а не полагаться
на файрвол как на единственный рубеж.

## Статус деплоя

Фактически подтверждено на VPS (реальный `docker compose up` + smoke-test,
не только статический `config`):

- **Контейнеры**: `nginx:1.30.4-alpine`, `postgres:16.14-alpine` и
  `nickfedor/watchtower:1.19.0` — запущены и в статусе `healthy`.
  `registry:3.1.1` — запущен и работает (у Registry по дизайну нет
  Docker-healthcheck, см. "Почему так" ниже, поэтому статуса `healthy`
  для него в принципе не бывает). `pgAdmin` запускается только через
  профиль `admin`, по требованию.
- **HTTPS**: Let's Encrypt сертификат выпущен на один SAN на оба домена
  (`vibe.elivcloud.org`, `registry-vibe.elivcloud.org`), монтируется в
  Nginx read-only из `/etc/letsencrypt`. `https://vibe.elivcloud.org`
  отвечает `200`. HTTP для обоих доменов редиректит на HTTPS, кроме
  ACME challenge и `/healthz`.
- **Registry**: пользователь создан через `registry/create-user.sh`,
  `docker login` в `registry-vibe.elivcloud.org` подтверждён. Запрос
  `/v2/` без авторизации возвращает `401` с Basic auth challenge — это
  ожидаемое поведение, подтверждающее, что и TLS, и auth реально
  работают. Полный push/pull smoke-test пройден: тестовый образ
  `hello-world` запушен в `registry-vibe.elivcloud.org/test/hello-world:latest`,
  локальный тег удалён, образ успешно вытянут обратно из приватного Registry.
- **PostgreSQL / pgAdmin**: 5432 наружу не публикуется (как и
  спроектировано). pgAdmin поднят через профиль `admin`, доступ
  проверен через SSH-туннель, подключение к PostgreSQL
  (`host: postgres`, `port: 5432`, `database: vibe_orders`) настроено и
  база видна в интерфейсе. После проверки pgAdmin остановлен.
- **Ресурсы**: четыре постоянно работающих контейнера (nginx, postgres,
  registry, watchtower) укладываются в заданные `deploy.resources.limits`
  с заметным запасом; pgAdmin поднимается только по требованию.

Не проверялось в рамках этого деплоя: реальный backend (не реализован),
HSTS (сознательно не включён), auto-update через Watchtower (opt-in
везде выключен по дизайну).

## Архитектура

```
Интернет
   │  80/443
   ▼
┌─────────┐        proxy-net        ┌──────────┐
│  Nginx  │◄───────────────────────►│ Registry │
└────┬────┘                         └──────────┘
     │ (после появления backend)
     │
     ▼                    app-net
┌──────────┐        ┌────────────┐        ┌─────────┐
│ backend  │◄──────►│ PostgreSQL │◄──────►│ pgAdmin │
│ (позже)  │        └────────────┘        └─────────┘
└──────────┘                                   ▲
                                                │ 127.0.0.1:5050
                                          SSH-туннель с локальной машины
```

Единственный сервис, публикующий порты на все интерфейсы (`0.0.0.0`), —
**Nginx** (80/443). Все остальные сервисы либо вообще без host-портов,
либо привязаны только к `127.0.0.1`.

Две Docker-сети разделяют внешний и внутренний контуры:

- **proxy-net** — Nginx + Registry. Registry не публикует порт наружу,
  но доступен Nginx по внутреннему DNS-имени `registry`.
- **app-net** — PostgreSQL + pgAdmin (в будущем — backend). Полностью
  отделена от контура, к которому имеет доступ интернет.

Backend в этой сети пока не реализован — в `docker-compose.yml` есть
только закомментированный шаблон, показывающий, что backend в будущем
будет подключён и к `app-net` (доступ к PostgreSQL), и к `proxy-net`
(чтобы Nginx мог проксировать запросы к нему).

## Обязательные переменные окружения (.env)

`.env` **обязателен** для запуска — без него `docker compose up`/`config`
откажется стартовать. Критичные переменные (учётные данные PostgreSQL,
учётные данные pgAdmin) объявлены в `docker-compose.yml` в форме
`${VAR:?VAR must be set}`: если переменная не задана, Compose падает с
понятной ошибкой вместо того, чтобы тихо подставить пустую строку
(что могло бы, например, создать PostgreSQL-пользователя с пустым
паролем).

Обязательны:
- `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`
- `PGADMIN_DEFAULT_EMAIL`, `PGADMIN_DEFAULT_PASSWORD` (нужны, только
  если запускается профиль `admin`, но объявлены обязательными и там)

Необязательна (есть безопасное значение по умолчанию):
- `WATCHTOWER_POLL_INTERVAL` (по умолчанию 86400с = раз в сутки)

Registry свои учётные данные из `.env` не берёт — см. следующий раздел.

Проверить конфигурацию без запуска контейнеров:
```bash
docker compose --env-file .env.example config
docker compose --env-file .env.example --profile admin config
```
(с `.env.example` эти команды проходят, потому что там есть все
обязательные переменные с безопасными плейсхолдерами; с реальным
`.env` — то же самое, но с настоящими значениями).

## Порядок деплоя (bootstrap)

Этот порядок уже пройден на текущем VPS (см. "Статус деплоя" выше) и
остаётся здесь как процедура для повторного/дополнительного деплоя.
Registry **не считается готовым к запуску** без файла
`registry/auth/htpasswd` — до его появления контейнер стартует, но
любой запрос к Registry будет отклонён на этапе аутентификации (это
ожидаемо и правильно, а не баг). Поэтому порядок действий важен:

1. **Создать `.env`** на VPS (`cp .env.example .env`, заполнить
   реальными значениями — НЕ теми, что в примере).
2. **Установить `apache2-utils`**, если `htpasswd` ещё не установлен:
   ```bash
   command -v htpasswd || sudo apt-get update && sudo apt-get install -y apache2-utils
   ```
3. **Запустить `registry/create-user.sh <username>`** (потребует
   `chmod +x` при первом запуске) — пароль вводится интерактивно и
   скрыто, скрипт его нигде не сохраняет.
4. **Убедиться, что файл создан**: `ls -l registry/auth/htpasswd`
   (права должны быть `600`, владелец — тот же пользователь, от
   которого будет запускаться `docker compose`).
5. **Только теперь** `docker compose up -d` (без профиля `admin` —
   см. ниже про pgAdmin).

Выпуск TLS-сертификатов и включение HTTPS — отдельный шаг после
bootstrap-порядка выше (certbot на VPS, затем перезапуск Nginx); на
текущем VPS уже выполнен, подробности — в разделе "HTTPS" ниже.

## Почему так

**PostgreSQL без внешнего порта.** 5432 не публикуется ни на
`0.0.0.0`, ни на `127.0.0.1`: субд не нужна нигде, кроме `app-net`.

**Registry без прямого доступа.** По тем же причинам порт 5000 не
публикуется. Доступ — только через Nginx по HTTPS
(`registry-vibe.elivcloud.org`), с Basic Auth на стороне самого
Registry (`REGISTRY_AUTH=htpasswd`, файл `registry/auth/htpasswd`,
создаётся через `registry/create-user.sh` — см. "Порядок деплоя" выше).
Nginx по-прежнему намеренно НЕ проксирует запросы к Registry по
обычному HTTP (порт 80 только редиректит на HTTPS) — иначе логин/пароль
передавались бы в открытом виде.

**Registry — подтверждено полным push/pull smoke-test'ом.** После
выпуска сертификата и создания пользователя через `create-user.sh`
проверен весь путь целиком: `docker login registry-vibe.elivcloud.org`
успешен; запрос `/v2/` без креденшлов возвращает `401` с Basic auth
challenge (ожидаемо — значит и TLS, и auth реально в деле, а не просто
запущены); тестовый образ `hello-world` запушен в
`registry-vibe.elivcloud.org/test/hello-world:latest`, локальный тег
удалён, образ успешно вытянут обратно — то есть цикл push → pull
через приватный Registry за HTTPS с auth работает end-to-end.

**Registry v3.1.1 — совместимость с текущей конфигурацией проверена**
перед апгрейдом с `registry:2.x` по официальной документации CNCF
Distribution: конвенция переменных `REGISTRY_<SECTION>_<KEY>`
(`REGISTRY_AUTH`, `REGISTRY_AUTH_HTPASSWD_REALM`,
`REGISTRY_AUTH_HTPASSWD_PATH`) и требование bcrypt-хэшей для htpasswd
не изменились; путь тома данных по умолчанию (`/var/lib/registry`) не
изменился. Путь конфига по умолчанию сменился на
`/etc/distribution/config.yml` (был `/etc/docker/registry/config.yml`
в v2), но на нас это не влияет — кастомный `config.yml` не
монтируется, используются только env var overrides. Отдельно: CLI-
утилита `htpasswd` убрана из самого образа `registry` — не проблема,
`registry/create-user.sh` использует `htpasswd` с хоста VPS
(`apache2-utils`), а не изнутри контейнера.

**pgAdmin — только localhost/SSH-tunnel, только по требованию.** Порт
публикуется как `127.0.0.1:5050:80`, то есть недоступен снаружи VPS в
принципе (не вопрос файрвола — Docker физически не слушает внешний
интерфейс). Доступ с локальной машины — через SSH-туннель, например:

```bash
ssh -N -L 15050:127.0.0.1:5050 vibe-vps
# затем открыть http://127.0.0.1:15050 в браузере на локальной машине
```

(Локальный порт туннеля не обязан совпадать с портом на VPS — в
примере выше `15050` использован потому, что `5050` на локальной
машине был занят; `vibe-vps` — алиас хоста из `~/.ssh/config`.)

Проверено на реальном деплое: через такой туннель в pgAdmin успешно
настроено подключение к PostgreSQL (`host: postgres`, `port: 5432`,
`database: vibe_orders` — `postgres` резолвится по имени сервиса
Docker, только изнутри `app-net`), база `vibe_orders` видна в
интерфейсе. После проверки pgAdmin остановлен, чтобы не расходовать
RAM впустую.

pgAdmin также вынесен в Compose-профиль `admin` и НЕ поднимается
командой `docker compose up` без указания профиля — чтобы не
потреблять RAM постоянно на VPS с ограничением ~1GB:

```bash
docker compose --profile admin up -d pgadmin
docker compose --profile admin stop pgadmin   # когда не нужен
```

**Watchtower присутствует, настроен, но пока никого не обновляет.**
`WATCHTOWER_LABEL_ENABLE=true` переключает Watchtower в режим
"обновляю только контейнеры с явным label
`com.centurylinklabs.watchtower.enable=true`". На данном этапе этот
label выставлен в `"false"` у **всех** инфраструктурных сервисов —
`nginx`, `postgres`, `registry`, `pgadmin`. Это осознанное
production-like решение, а не недосмотр:

- PostgreSQL — обновление СУБД в фоне может уронить данные заявок или
  тихо сломать совместимость данных с новой мажорной версией.
- Registry и Nginx — единственные, кто напрямую держит порты наружу
  (пусть и разными путями); их обновление предпочтительно делать
  контролируемо, синхронно с проверкой, что сервис поднялся корректно.
- pgAdmin — админ-панель с доступом к БД; обновлять без присмотра
  не нужно, тем более что она и так поднимается только по требованию.

**Watchtower-образ: `nickfedor/watchtower`, а не `containrrr/watchtower`.**
Оригинальный `containrrr/watchtower` — заархивирован (read-only с
17 декабря 2025), релизы, багфиксы и security-патчи прекращены.
Дополнительно он несовместим с современным Docker Engine 29: падает
на старте с ошибкой `client version 1.25 is too old. Minimum supported
API version is 1.44`. `nickfedor/watchtower` — активно поддерживаемый
форк того же проекта, обновляющий внутренние зависимости под текущий
Docker API. Перед переключением образа проверена (по официальной
документации форка, watchtower.nickfedor.com) совместимость всех
используемых здесь переменных окружения — `WATCHTOWER_LABEL_ENABLE`,
`WATCHTOWER_CLEANUP`, `WATCHTOWER_POLL_INTERVAL` — задокументированы в
форке с теми же именами, типами и значениями по умолчанию, что и в
оригинале; изменений в `environment`/`command` не потребовалось. Сама
логика label-based opt-in и текущее состояние "все opt-in = false"
(описанные выше) не менялись — заменился только образ.

В будущем opt-in (`enable: "true"`) разумно включить точечно для
**stateless backend**, когда он появится: его проще безопасно
пересоздавать автоматически, чем сервисы с состоянием или
привилегированным сетевым положением. Закомментированный пример
backend-сервиса в `docker-compose.yml` уже показывает это как
рекомендацию на будущее.

**Риск docker.sock.** Watchtower должен уметь пересоздавать
контейнеры, поэтому ему смонтирован `/var/run/docker.sock`. Это
даёт контейнеру фактически root-доступ к хосту: через Docker API
можно запустить произвольный привилегированный контейнер с
бинд-маунтом `/`. Флаг `:ro` на монтировании ограничивает только
возможность подменить/удалить сам файл сокета — он **не** ограничивает
вызовы Docker API через этот сокет. Это осознанный компромисс,
типичный для Watchtower, и в этом учебном проекте он сохраняется
специально — задание явно требует Watchtower в составе инфраструктуры.
В проде эту роль обычно возлагают на более узкоправный socket-proxy
(например, `tecnativa/docker-socket-proxy`) — здесь такой прокси
сознательно не добавлен, чтобы не усложнять каркас.

**HTTPS.** Настроен и подтверждён на реальном деплое. Let's Encrypt
выпустил один сертификат на оба домена (`vibe.elivcloud.org` +
`registry-vibe.elivcloud.org` как SAN); сертификат физически лежит на
VPS под `/etc/letsencrypt/live/vibe.elivcloud.org/` и монтируется в
Nginx **read-only** напрямую оттуда (`/etc/letsencrypt:/etc/letsencrypt:ro`
в `docker-compose.yml`) — в репозиторий не копируется и не хранится.
`nginx/conf.d/*.conf` для обоих доменов: порт 80 отдаёт ACME challenge
(`/.well-known/acme-challenge/`, нужен certbot'у для продления) и
`/healthz` (для внутреннего Docker-healthcheck), весь остальной HTTP
редиректит на HTTPS (`301`); порт 443 — активен, `ssl_protocols
TLSv1.2 TLSv1.3` заданы один раз глобально в `nginx.conf`. Registry на
443 проксируется с registry-specific настройками
(`proxy_http_version 1.1`, `proxy_request_buffering off`, увеличенные
таймауты, заголовок `Docker-Distribution-Api-Version`, корректные
`X-Forwarded-*`). Подтверждено: `https://vibe.elivcloud.org` → `200`;
`https://registry-vibe.elivcloud.org/v2/` без креденшлов → `401` с
Basic auth challenge (см. "Статус деплоя" выше).
`nginx/certs/` в репозитории — неиспользуемый пустой плейсхолдер
(остался от более раннего варианта, где сертификаты предполагалось
класть в bind-mount внутри репозитория; фактически используется
`/etc/letsencrypt` на хосте, как описано выше).
HSTS сознательно **не включён** — его стоит добавлять отдельным шагом
после более длительного периода стабильной работы HTTPS, а не сразу
после первого smoke-test.

**Nginx hardening.** `server_tokens off;` (не светить версию Nginx),
`X-Content-Type-Options: nosniff` и `Referrer-Policy` добавлены во все
активные и будущие server-блоки. Обратите внимание: `add_header`
внутри `location {}` сбрасывает наследование `add_header` из
родительского `server {}` (особенность Nginx) — поэтому в
registry-конфиге эти заголовки продублированы внутри `location /v2/`,
а не полагаются на наследование с уровня `server`.

## Resource protection (~1GB RAM VPS)

Лимиты заданы через `deploy.resources.limits` — это часть Compose
Specification и применяется обычным `docker compose up` (swarm не
нужен; проверено рендерингом через `docker compose config`, см. ниже).
Заданы для всех пяти сервисов: nginx, postgres, registry, pgadmin,
watchtower.

| Сервис     | memory limit | memory reservation | cpus |
|------------|-------------:|--------------------:|-----:|
| postgres   | 384M | 192M | 1.00 |
| pgadmin    | 256M | 128M | 0.50 |
| registry   | 192M |  64M | 0.50 |
| nginx      |  96M |  32M | 0.50 |
| watchtower | 128M |  32M | 0.25 |

PostgreSQL намеренно получает наибольшую долю (лимит не занижен
агрессивно) — слишком туго ограниченная СУБД гарантированно упадёт по
OOM под нагрузкой, что хуже, чем не ограничивать её вовсе. Сумма
лимитов сервисов, работающих по умолчанию (без профиля `admin`):
postgres + registry + nginx + watchtower = 384+192+96+128 = **800M**,
оставляя ~200M+ на ОС и сам Docker daemon на VPS с ~1GB RAM. При
включённом профиле `admin` (+256M pgadmin) суммарный потолок
приближается к 1GB — это ожидаемо и является ровно той причиной,
по которой pgAdmin держат за профилем и останавливают после
использования, а не постоянно включённым.

Лимиты — это потолок (cgroup limit), а не резервирование: контейнер
обычно потребляет значительно меньше и не блокирует эту память для
других, пока не использует её сам. Подтверждено на реальном деплое:
все четыре постоянно работающих контейнера держатся заметно ниже
своих лимитов.

**Log rotation.** Все пять сервисов используют `logging: driver:
json-file` с `max-size: "10m"`, `max-file: "3"` — до ~30MB логов на
контейнер, дальше старые файлы ротируются. Без этого логи Docker
могут неограниченно расти и забить диск VPS. (Для Nginx это работает
благодаря тому, что образ `nginx:alpine` по умолчанию симлинкует
`/var/log/nginx/access.log`/`error.log` на `/dev/stdout`/`stderr` —
наш `nginx.conf` эти пути не переопределяет, так что логи реально
попадают в Docker log driver, а не теряются внутри контейнера.)

## Версии образов

Все образы зафиксированы на конкретных версиях (без `:latest`):

| Сервис | Образ |
|---|---|
| postgres | `postgres:16.14-alpine` |
| pgadmin | `dpage/pgadmin4:9.16` |
| registry | `registry:3.1.1` |
| nginx | `nginx:1.30.4-alpine` |
| watchtower | `nickfedor/watchtower:1.19.0` |

Ранее `pgadmin`/`watchtower` были временно помечены
плейсхолдер-тегами (не хватало внешней проверки версий) — теперь
зафиксированы конкретные версии; совместимость Watchtower-форка и
Registry v3.1.1 с текущей конфигурацией проверена по официальной
документации (см. разделы "Watchtower" и "Registry v3.1.1 —
совместимость..." выше) перед указанием версий в `docker-compose.yml`.

## Отличия от буквальной формулировки задания

Учебное задание допускает `pgAdmin` через `127.0.0.1:5050:80` — это и
реализовано буквально. Дополнительно (сверх минимума) сделано:

- pgAdmin вынесен в отдельный Compose-профиль, а не просто "порт на
  localhost", чтобы он не грузил RAM, когда не используется.
- Basic Auth для Registry реализован на стороне самого Registry
  (встроенная поддержка `htpasswd`/bcrypt), а не через `auth_basic` в
  Nginx — меньше движущихся частей, один источник правды для
  учётных данных.
- В `create-user.sh` проверка существующего пользователя — точное
  сравнение поля через `awk`, а не `grep -E` (спецсимволы в имени
  пользователя, например `.`, иначе интерпретировались бы как
  regex); добавлены резервное копирование `htpasswd` перед правкой и
  явное подтверждение при обновлении существующего пользователя.
- Watchtower по умолчанию не обновляет НИ ОДИН инфраструктурный
  контейнер (все label = `false`) — более консервативно, чем
  буквально требовал первый проход задания (тогда `nginx`/`registry`
  были включены); см. "Почему так" выше.
- Осознанно НЕ включён `internal: true` на Docker-сетях (хотя это
  дополнительно ужесточило бы изоляцию `app-net`/`proxy-net`), так как
  это взаимодействует с публикацией портов не всегда очевидным
  образом. Реальный деплой прошёл без этой настройки, поэтому её
  влияние на публикацию портов (в частности, `127.0.0.1:5050` у
  pgAdmin) так и не проверялось на практике. Возможное улучшение на
  будущее, требующее отдельной проверки перед включением.

## Структура

```
vibe-order-infra/
├── docker-compose.yml
├── .env.example
├── .gitignore
├── README.md
├── nginx/
│   ├── nginx.conf
│   ├── conf.d/
│   │   ├── vibe.elivcloud.org.conf
│   │   └── registry-vibe.elivcloud.org.conf
│   ├── acme-challenge/        # ACME HTTP-01 webroot (в репозитории пусто, certbot кладёт файлы во время выпуска/продления)
│   └── certs/                 # неиспользуемый плейсхолдер — реальные сертификаты монтируются из /etc/letsencrypt на хосте, см. раздел "HTTPS"
└── registry/
    ├── create-user.sh         # создание/обновление пользователя Registry
    └── auth/                  # htpasswd (создаётся скриптом на VPS, не в git)
```

## Дальше по плану

Bootstrap, HTTPS и Registry-smoke-test уже выполнены на VPS (см.
"Статус деплоя"). Из содержательного здесь остаётся только:

1. Реализовать backend, подключить его к `app-net` (доступ к
   PostgreSQL) и `proxy-net` (чтобы Nginx мог проксировать запросы) —
   заготовка уже есть в `docker-compose.yml` (закомментированный
   пример сервиса).
2. После появления backend — заменить placeholder-`location /` на
   реальный `proxy_pass` в HTTPS-блоке `vibe.elivcloud.org.conf`.
3. Рассмотреть точечный opt-in Watchtower-label именно для backend
   (stateless, безопаснее автообновлять), не трогая остальные сервисы.
4. Продление сертификата Let's Encrypt — автоматизировать (cron/systemd
   timer с `certbot renew`) отдельным шагом; в рамках этого деплоя
   настраивался только первичный выпуск.
5. HSTS — включить отдельным шагом после более длительного периода
   стабильной работы HTTPS (см. раздел "HTTPS" выше).
